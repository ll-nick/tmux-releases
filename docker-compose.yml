services:
  tmux-builder:
    image: tmux-builder
    container_name: tmux-builder
    build:
      args:
        - TMUX_VERSION=${TMUX_VERSION}
        - MUSL_VERSION=${MUSL_VERSION}
        - LIBEVENT_VERSION=${LIBEVENT_VERSION}
        - NCURSES_VERSION=${NCURSES_VERSION}
        - PREFIX=/build
